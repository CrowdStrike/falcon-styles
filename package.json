{
  "name": "falcon-styles",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build-transform-system": "node node_modules/token-transformer/cli tokens.json tokens/system.json reference,system reference",
    "build-transform-light": "node node_modules/token-transformer/cli tokens.json tokens/light.json reference,light,light-component reference",
    "build-transform-dark": "node node_modules/token-transformer/cli tokens.json tokens/dark.json reference,dark,dark-component reference",
    "build-transform": "pnpm run build-transform-system && pnpm run build-transform-light && pnpm run build-transform-dark",
    "build-tokens": "node build-tokens.js",
    "build": "pnpm build-transform && pnpm build-tokens && vite build",
    "dev": "vite",
    "preview": "vite preview",
    "watch": "chokidar  \"tokens.json\" -c \"pnpm run build\""
  },
  "devDependencies": {
    "modern-normalize": "^2.0.0",
    "@tokens-studio/sd-transforms": "^0.8.5",
    "chokidar-cli": "^1.2.0",
    "style-dictionary": "^3.8.0",
    "token-transformer": "^0.0.32",
    "vite": "^4.3.9"
  }
}
